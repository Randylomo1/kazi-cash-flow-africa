
<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/kaziBluePrimary</item>
        <item name="colorPrimaryDark">@color/kaziBlueDark</item>
        <item name="colorAccent">@color/kaziOrange</item>
        <item name="android:windowBackground">@color/background</item>
    </style>

    <!-- Splash screen theme -->
    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/kaziBluePrimary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>
</resources>
